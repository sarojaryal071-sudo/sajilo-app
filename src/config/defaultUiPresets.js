// Fallback preset registry – a preset is a complete UI configuration snapshot.
export const defaultUiPresets = {
  default: {
    tokens: {},
    branding: {},
    variants: {},
    layout: {},
  },
};