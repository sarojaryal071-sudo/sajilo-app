// Fallback content structure – used when no content is published.
// All values are intentionally empty; real content is managed via UI Studio.
export const defaultUiContent = {
  labels: {},
  buttons: {},
  headings: {},
  banners: {},
  emptyStates: {},
  tooltips: {},
};